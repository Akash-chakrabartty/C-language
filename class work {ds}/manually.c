#include <stdio.h>
#include <stdlib.h>
struct node
{
    int data;
    struct node *next;
} struct node *newnode(struct node *) malloc(sizeof(struct node));
newnode1->data = 10;
newnode1->next = 0;